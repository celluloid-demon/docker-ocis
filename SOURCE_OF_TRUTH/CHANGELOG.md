# Changelog

- Changed `docker-compose.yml` to `docker-compose.ORIGINAL.yml`
- Added (new) `docker-compose.yml`

- Added `docs`
- Added `.gitignore`
- Added `overrides`
- Added `SOURCE_OF_TRUTH`
- Changed `.env` to `example.env`
