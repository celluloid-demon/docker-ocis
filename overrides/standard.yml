---
services:
  ocis:
    volumes:
      # - ${VOLUMES}/config/ocis/app-registry.yaml:/etc/ocis/app-registry.yaml
      # - ${VOLUMES}/config/ocis/csp.yaml:/etc/ocis/csp.yaml
      # - ${VOLUMES}/config/ocis/banned-password-list.txt:/etc/ocis/banned-password-list.txt
      - ${VOLUMES}/config:/etc/ocis
      - ${VOLUMES}/data:/var/lib/ocis
